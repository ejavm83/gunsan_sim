# Streamlit Community Cloud는 이 파일만 설치합니다.
# Python: 로그에 3.14.x가 나오면 휠/호환 문제로 "in the oven"에서 멈출 수 있다.
#   Cloud 앱 설정 → Advanced settings → Python 3.12 선택.
#   버전 변경이 안 되면 앱 삭제 후 같은 저장소로 새로 배포(또는 체크리스트 APP_DEPLOY_CHECKLIST.txt).
# 앱 진입: webapp.py 또는 streamlit_app.py (main.py는 CLI용이나 Cloud 오설정 시 webapp으로 위임).
# 로컬 전체(CLI·docx·쿠키 등): pip install -r requirements-cli.txt
simpy>=4.1,<5
plotly>=5.18,<6
pandas>=2.0,<2.3
numpy>=1.26,<3
Pillow>=10,<12
streamlit>=1.45.1,<2
reportlab>=4.0,<4.5
